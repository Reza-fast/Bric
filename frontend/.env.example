# Optional: if the API is not on 127.0.0.1:4000, set the rewrite target (see next.config.ts)
# API_PROXY_TARGET=http://127.0.0.1:4000

# Must match backend AUTH_COOKIE_NAME if you change the default (bric_token)
# NEXT_PUBLIC_AUTH_COOKIE_NAME=bric_token
